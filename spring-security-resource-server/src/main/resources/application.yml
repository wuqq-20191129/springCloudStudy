server:
  port: 8071
spring:
  application:
    name: resource-server
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/oauth2_resource?characterEncoding=utf8&useUnicode=true&useSSL=false
    username: root
    password: 142845wer
    type: com.alibaba.druid.pool.DruidDataSource
    druid:
      max-wait: 10000
      max-active: 20
      min-idle: 10
      initial-size: 2
      validation-query: SELECT 1 FROM DUAL
      test-on-return: false
      test-on-borrow: false
      test-while-idle: true
      web-stat-filter:
        enabled: true
        exclusions: js,gif,jpg,png,css,ico,/druid/*
      stat-view-servlet:
        enabled: true
        login-username: druid@wuqq
        login-password: druid@wuqq
mybatis-plus:
  mapper-locations: classpath:mapper/**/*.xml
  type-aliases-package: com.wuqq.oauth2.entity
  global-config:
    db-config:
      id-type: auto
      logic-delete-value: 1
      logic-not-delete-value: 0
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
security:
  oauth2:
    client:
      client-id: client
      client-secret: secret
      access-token-uri: http://localhost:8099/wuqq/oauth/token
      user-authorization-uri: http://localhost:8099/wuqq/oauth/authorize
    resource:
      token-info-uri: http://localhost:8099/wuqq/oauth/check_token